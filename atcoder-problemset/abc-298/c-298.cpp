#include <iostream>
#include <vector>

using namespace std;

int main(){
    int boxes, queries, in;
    cin >> boxes >> queries;

    vector<vector<int>> ans;
    
    while(queries--){
        cin >> in;
        if(in == 1) {
            int a, b;
            cin >> a >> b;
            
        } else if(in == 2) {
            int a;
            cin >> a;
        } else {
            int a;
            cin >> a;
        }
    }
    
    return 0;
}